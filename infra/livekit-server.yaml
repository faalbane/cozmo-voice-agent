port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: true

redis:
  address: redis:6379

keys:
  # Override these via environment or .env in production
  devkey: secret

logging:
  level: info
  pion_level: error

room:
  empty_timeout: 300
  max_participants: 2  # Agent + caller per room

webhook:
  urls: []

sip:
  enabled: true
